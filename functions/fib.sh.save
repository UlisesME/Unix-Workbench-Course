function fib {
	local prev=0
	local next=1
	local current=0
	local count=0
	while [[ $count -le $1 ]]
	do
		let count=$count		echo $prev
		let current=$prev+$next 
		let prev=$next
		let next=$current
	done
}
